# vitaS — متجر الألبسة الطبية (Medical Scrubs Store)

## المشروع
موقع متجر إلكتروني لبيع الألبسة الطبية (سكراب) باسم **vitaS**، يعرض المجموعة الأساسية
والإصدارات الخاصة بهذا المجال.

## المهارات المعتمدة لهذا المشروع (Skills)

### مثبّتة داخل المشروع
- **`frontend-design`** → `.claude/skills/frontend-design/`
  المهارة المرجعية للتصميم البصري. مُثبّتة محلياً لأنها غير مفعّلة على مستوى الحساب.
  تُستخدم في كل عمل على الواجهة: اتجاه جمالي، طباعة، بناء نظام tokens، ونقد ذاتي
  قبل وبعد الكتابة.

### مفعّلة على مستوى الجلسة (تُستدعى ولا تُنسخ)
| المهارة | الاستخدام في هذا المشروع |
|---|---|
| `design:design-critique` | مراجعة الهرمية والاتساق بعد كل تغيير كبير على الواجهة |
| `design:accessibility-review` | تدقيق WCAG 2.1 AA: تباين الألوان، التنقل بلوحة المفاتيح، حجم مناطق اللمس |
| `design:ux-copy` | صياغة نصوص الأزرار، رسائل الخطأ، الحالات الفارغة، وأزرار الشراء |
| `design:design-system` | توثيق وتوسيع نظام الـ tokens والمكوّنات |
| `design:design-handoff` | إخراج مواصفات التسليم للمطوّرين عند الحاجة |
| `artifact-design` | يُحمّل قبل نشر أي صفحة كـ Artifact |

> لا تُنسخ المهارات المفعّلة إلى المشروع — النسخ يضيف ضجيجاً بلا قدرة إضافية.

## قرارات ثابتة (لا يُعاد فتحها بدون طلب المستخدم)

1. **اللغة والاتجاه**: عربي أولاً، `lang="ar"` و `dir="rtl"`.
   استخدم دائماً **CSS logical properties**
   (`margin-inline-start`, `padding-inline`, `inset-inline`, `border-inline-start`)
   ولا تستخدم `left/right` إطلاقاً في التخطيط.
   العلامة التجارية `vitaS` تبقى لاتينية دائماً وبهذا الرسم بالضبط.
2. **التقنية**: React 19 + Vite + Tailwind CSS v4 + **HeroUI v3** (`@heroui/react`).
   يتطلّب `npm install` ثم `npm run dev` — لم يعد يُفتح `index.html` مباشرة.
   الموقع الثابت السابق محفوظ كما هو في `legacy/` للمقارنة.
3. **الصور**: رسوم SVG للقطع مكتوبة يدوياً وقابلة لإعادة التلوين عبر متغيّرات CSS.
   **ممنوع** استخدام مربّعات رمادية كعناصر نائبة.
4. **العملة**: الدينار العراقي `د.ع`. التنسيق عبر `price()` في `core.js` — **لا** تُضاف
   صيغة عملة إلى `money()` لأنها مشتركة مع الوزن والجيوب والكمّية وأرقام المخطّط.

## الاتجاه البصري
المرجع هو **الـ tech pack** (ملف المواصفات الفنية للملابس) وورق الباترون،
لا الموقع الطبي النموذجي الأبيض/الفيروزي.

- **اللوحة**: ورق باترون `--paper` · حبر أخضر داكن `--ink` · أحمر علّامة `--marker`
  · أزرق مخطّطات `--blueprint` · أخضر جراحي `--scrub`
- **الخطوط**: Reem Kufi (عرض) · IBM Plex Sans Arabic (متن) · IBM Plex Mono (أرقام ومواصفات)
- **العنصر المميّز**: المخطّط الفني التفاعلي للسكراب مع أرقام إحالة (callouts) تشرح الجيوب.
  الترقيم هنا مشروع لأنه يحمل معلومة إحالة حقيقية كما في الرسم الهندسي — لا كزينة.

### فخاخ يجب تجنّبها
- الأبيض + الفيروزي/النعناعي مع خط sans طبي — القالب السائد لمواقع السكراب.
- الأنماط الثلاثة الشائعة في تصاميم الذكاء الاصطناعي: كريمي `#F4F1EA` + serif + طيني؛
  أسود + أخضر حمضي؛ تخطيط صحيفة بخطوط شعرية.
- خطوط Cairo / Tajawal — الخيار الافتراضي لأي موقع عربي.
- ترقيم `01 / 02 / 03` الزخرفي في الأقسام التي ليست تسلسلاً فعلياً.

## البنية
```
index.html               صدفة Vite
public/img/              صور الموديلات (١٢ صورة)
legacy/                  الموقع الثابت السابق — مرجع لا يُبنى

src/data/catalog.js      المنتجات والألوان والمقاسات والصور
src/lib/format.js        money / price / pockets
src/lib/garments.jsx     رسوم SVG للقطع
src/lib/cart.jsx         CartProvider + التوصيل والمحافظات
src/lib/router.jsx       موجّه بالهاش بلا اعتماد خارجي
src/lib/useSelection.js  حالة اختيارات المنتج + مفتاح السلة
src/lib/content.jsx      نصوص الواجهة ورؤية الأقسام (ContentProvider)
src/lib/orders.js        قراءة/كتابة الطلبات وحالاتها
src/components/          Chrome · Gallery · ProductDetail · QuickView · CartDrawer
src/pages/               Home · Product · Checkout · Admin
src/theme.css            Tailwind + HeroUI + هوية vitaS
```

المسارات: `#/` و `#/product/VS-01` و `#/checkout` و `#/admin`.

## التشغيل
الموقع ثابت بالكامل — يكفي فتح `index.html` مباشرة في المتصفّح.
ولتشغيله عبر خادم محلي (مفيد عند إضافة أي طلب شبكة لاحقاً):

```bash
python3 -m http.server 8125 --directory /Users/docrep/Documents/vitaS
```

مُعرَّف أيضاً في `.claude/launch.json` باسم `vitaS`.

## ملاحظات من جولة التصميم الأولى (٢٢ أغسطس ٢٠٢٦)

### فخاخ RTL ظهرت فعلياً أثناء التحقّق البصري — لا تكرّرها
- **`vitaS` ظهرت `Svita`**: اسم لاتيني داخل فقرة RTL يُعاد ترتيبه بخوارزمية bidi.
  الحل: `dir="ltr"` + `unicode-bidi: isolate` على `.brand`.
- **`٦٠°` ظهرت `°٦٠`**: رمز الدرجة محايد الاتجاه بجوار أرقام عربية.
  الحل: إزالة الرمز ونقل المعنى إلى النص («درجة حرارة الغسيل»).
- **`عادي 30"` ظهرت معكوسة**: علامة البوصة `"` + أرقام لاتينية داخل نص عربي.
  الحل: `عادي ٣٠ بوصة`.
- **حشوة فيزيائية مع حدّ منطقي**: `padding: 1rem 1.3rem 1rem 0` مع
  `border-inline-end` توقع المسافة على الجانب المقابل للحدّ في RTL.
- الأرقام: الكميات والقياسات بأرقام عربية عبر `money()`،
  وأكواد المنتجات (`VS-01`) لاتينية — هذا التمييز مقصود.
- إحداثيات نقاط المخطّط تستعمل `left` الفيزيائية عمداً لأنها تُركَّب فوق
  رسم SVG بإحداثيات ثابتة لا تنعكس مع الاتجاه.

### فخّ الخطوط: العربية داخل الخط أحادي العرض
`IBM Plex Mono` لا يحوي حروفاً عربية إطلاقاً، فكان كل نص عربي بصنف `.mono`
— وهي أشرطة الأقسام، ورأس المخطّط، وجدول المقاسات، وعناوين التذييل، ورسالة الخطأ —
يسقط على خط النظام، ثم يُطبَّق عليه `letter-spacing` فيفكّك الحروف المتّصلة.
(قياس السلسلة نفسها: ١٥٣٫٦ بكسل في مكدّس mono مقابل ١١٤٫٣ في مكدّس المتن.)

الحل: `--mono: 'IBM Plex Mono', 'IBM Plex Sans Arabic', ui-monospace, monospace`
— البديل يتم لكل حرف على حدة، فتذهب اللاتينية والأرقام إلى Mono والعربية إلى
Plex Sans Arabic. وأُزيل `letter-spacing` من `.mono` ومن `.eyebrow`، وحُصر في
صنف `.code` الذي يوضع على أكواد المنتجات اللاتينية وحدها.

**القاعدة**: لا تضع `letter-spacing` على أي عنصر قد يحمل نصاً عربياً.

### مسائل تخطيط عولجت
- شبكة المنتجات: حيلة `gap:1px` + خلفية الحاوية كانت تُظهر الخلايا الفارغة
  ككتلة رمادية. الحدود الآن على البطاقات نفسها.
- شبكة الإصدارات: `minmax(246px, 1fr)` ليدخل الأربعة في صف واحد على سطح المكتب
  بدل ثلاثة وبطاقة يتيمة ممدودة.
- خط التمييز تحت كلمة العنوان: `background-image` + `box-decoration-break: clone`
  بدل `box-shadow` الذي ينفصل عن الكلمة عند التفاف السطر.

### حالة التحقّق
- تباين الألوان: **صفر إخفاقات** على ٢٠٦ عقد نصية مقابل WCAG AA (٤٫٥ للنص الصغير).
  لونا `ED-02` و `ED-03` كانا ٢٫٧٠ و ٢٫٧٥ فوق الأرضية الداكنة وعُدِّلا.
  استعمل `--marker-lift` لا `--marker` فوق أي أرضية داكنة.
- مناطق النقر: **صفر عناصر** تحت ٢٤×٢٤ بكسل.
- لوحة المفاتيح: التركيز ينتقل إلى الدرج، محصور داخله، `Esc` يغلق ويعيد التركيز
  إلى الزر الذي فتحه. والأدراج المغلقة **خارج** ترتيب التنقّل — الإخفاء بالإزاحة
  وحده كان يترك ١٨ زراً قابلاً للتركيز خارج الشاشة، فأُضيفت `visibility: hidden`
  ضمن الانتقال (تتدرّج على خطوتين فلا تقطع حركة الانزلاق).
- لا تمرير أفقي على ٣٧٥ بكسل؛ جدول المقاسات يمرّر داخل حاويته وحدها.
- `prefers-reduced-motion` مُحترم.

## صفحة المنتج: المعرض والقَصّة (أُضيفت ٢٢ أغسطس ٢٠٢٦)

البطاقة في الشبكة **محايدة الجنس** (رسم القطعة مسطّحاً). التمييز يبدأ عند فتح المنتج:
مبدّل «القَصّة» (نسائي / رجالي) يُبدّل صور الموديل، ويُحفظ الاختيار لكل منتج في
`state.fitPick` فيعود كما تركه المستخدم.

### مصدر الصور
صور فوتوغرافية حقيقية من مكتبة Freepik المرتبطة بحساب المستخدم — **تنزيلها مجاني**
(تحقّقنا: الرصيد لم يتغيّر بعد التنزيل)، بخلاف التوليد الذي يكلّف ٦٠ نقطة للصورة الواحدة
بينما الرصيد المتبقّي ٧١ نقطة فقط.

الأصول نُزّلت بدقّتها الكاملة (١٢٢ ميغابايت) ثم صُغّرت بـ `sips` إلى ١١٠٠ بكسل
وجودة ٦٨ → **٩٦٠ كيلوبايت** للاثنتي عشرة صورة.

```bash
sips -Z 1100 -s format jpeg -s formatOptions 68 IMG.jpg --out IMG.jpg
```

### حدّ لا يمكن تجاوزه بالتصوير
المنتج الواحد له حتى ٦ ألوان، والتصوير لا يغطّيها كلها (٦ ألوان × قَصّتين × لقطتين
= ٢٤ جلسة تصوير للقطعة الواحدة). لذلك:

- كل صورة تحمل حقل `color` = **اللون الظاهر فيها فعلاً**، ويُعرض في تعليق تحت المعرض.
- لقطة **«القطعة مسطّحة»** هي المرجع اللوني الصحيح: ترسم SVG وتتبع اللون المختار فوراً.
- لا تدّعِ أبداً أن صورة الموديل تمثّل اللون المختار.

### استبدال الصور بتصوير حقيقي
عدّل `PHOTOS` في `assets/js/catalog.js` فقط — لا تلمس منطق العرض:

```js
'w-scrub-teal-a': { src: 'assets/img/VS-01-women-front.jpg', color: 'أزرق كاريبي', alt: '…' }
```

التسمية المقترحة للتصوير الجديد: `assets/img/{code}-{fit}-{view}.jpg`
مثل `VS-01-women-front.jpg`. الحقل `alt` **إلزامي** — تدقيق الوصولية يفحصه.

### `object-fit: contain` وليس `cover`
نسب الصور تتراوح بين ٠٫٦٧ و ٠٫٨٧ بينما لوح العرض ٠٫٧٥، فـ `cover` كان يقصّ من
أعلى/أسفل في إحدى عشرة صورة ومن الجانبين في صورة واحدة — أي يقصّ الثوب المعروض
للبيع نفسه. **لا يُقصّ ثوب في معرض منتج.** الفراغ الجانبي يقع على `--paper-2`
فيقرأ كخلفية استوديو.

### الترخيص — مهمّة مفتوحة قبل النشر
الصور من Freepik بترخيص مجاني، وهو **يشترط ذكر المصدر**. أُضيف سطر في التذييل،
لكن قبل أي نشر عام تحقّق من الصيغة المطلوبة لكل صورة أو استبدلها بتصوير مملوك.

### قرارات ثبتت هنا
- **`state.fit` جزء من مفتاح السلة.** بدونه ينهار «نسائي M» و«رجالي M» في سطر واحد
  بكميتين. القَصّة تظهر أيضاً في سطر السلة وفي إشعار الإضافة.
- **`data-product` لا `data-open`** لزر فتح المنتج: `data-open` مستعمل على الأدراج،
  و `closest('[data-open]')` كان يلتقط الاثنين.
- صور المصغّرات `alt=""` (زخرفية) والوصف على الزر نفسه عبر `aria-label` — الوصف
  مرة واحدة لا مرتين.
- جدول المقاسات واحد للقَصّتين (قياسات جسم لا قطعة). إن أردت جدولين، اطلب ذلك صراحة.
- القَصّة الافتراضية عند فتح منتج جديد هي **نسائي** (`state.fit` في `app.js`) —
  اختيار اعتباطي، غيّره إن أردت البدء بالرجالي.

### حالة التحقّق بعد الإضافة
- ١٢ صورة تُحمّل بلا كسر، وكل المنتجات العشرة لها صور للقَصّتين.
- تباين الألوان ومناطق النقر: **صفر إخفاقات** والدرج مفتوح.
- السلة: «نسائي M» و«رجالي M» سطران منفصلان، وإعادة إضافة نفس المتغيّر تزيد الكمية.

## المتجر متعدّد الصفحات (٢٢ أغسطس ٢٠٢٦)

- **صفحة منتج كاملة**: `product.html?code=VS-01`. كود مفقود أو مجهول يعرض حالة
  «لم نجد هذه القطعة» لا صفحة بيضاء.
- **السلة تُحفظ في `localStorage`** بمفتاح `vitas.cart.v1` — بدونها تضيع عند الانتقال
  بين الصفحات. عند تعذّر التخزين تعمل من الذاكرة عبر `Cart.memoryOnly`.
- **إتمام الطلب**: عنوان توصيل عراقي كامل — محافظة، مدينة، **أقرب نقطة دالّة**
  (إلزامية: المندوب يصل بها قبل اسم الشارع)، تفاصيل العنوان، هاتف بصيغة
  `07XXXXXXXXX`. الدفع عند الاستلام فقط.
- **أجرة التوصيل**: بغداد ٥٬٠٠٠ · بقيّة المحافظات ١٠٬٠٠٠ · مجاني فوق ٢٠٠٬٠٠٠.
- **الطلب لا يغادر المتصفّح**: يُحفظ في `vitas.orders.v1` ويُعرض تأكيد برقم طلب.
  لا شبكة ولا طرف ثالث ولا بوابة دفع.

### أخطاء عولجت هنا
- `wireCartControls` كانت تُربط مرّتين (من `bootShop` ومن `checkout.js`) فتقفز
  الكمّية خطوتين لكل نقرة. الربط الآن مرّة واحدة عبر `cartWired` مع قائمة `cartHooks`.
- معالِج الكمّية كان يفترض وجود السطر؛ صار يتحقّق قبل قراءة `qty`.
- `syncCart` كانت تفترض وجود `#ct-foot` و `#ct-total` — وهما في الدرج فقط لا في
  صفحة إتمام الطلب.
- لوح المعرض بنسبة ٣:٤ على عمود ٦٤٠ بكسل كان يعطي طولاً ٨٥٠ يبتلع الشاشة؛
  صار له سقف `min(68vh, 620px)`.

## النقل إلى React + HeroUI v3 (٢٢ أغسطس ٢٠٢٦)

بطلب المستخدم، بعد بيان أن HeroUI لا يعمل بلا React وخطوة بناء.

### ما بقي من الهوية
اللوحة والخطوط ورسوم SVG للقطع كلها انتقلت كما هي. الزوايا الحادّة
(`--radius: 0px`) جزء من لغة ملف المواصفات الفنية ولا تُدوَّر.

### ربط متغيّرات HeroUI — الفخّ الأساسي
- **`--accent` يقود الزر الأساسي** في HeroUI، لا `--default`.
  في vitaS الزر الأساسي حبر داكن يتحوّل إلى أحمر العلّامة عند المرور.
- **`--default-foreground` هو لون نصّ الأزرار المفرغة** (outline / ghost).
  ضبطه على لون الورق جعلها **ورقاً على ورق — غير مرئية إطلاقاً** (تباين ١:١).

### قواعد HeroUI v3 التي تخالف v2
- `onPress` لا `onClick`.
- لا `HeroUIProvider`.
- مكوّنات مركّبة: `Drawer > Drawer.Backdrop > Drawer.Content > Drawer.Dialog`.
- `TextArea` عنصر عارٍ — يُلَفّ بـ `TextField` للتسمية والتحقّق.

### أخطاء عولجت أثناء النقل
- **تعريف مكوّن `Field` داخل دالة العرض** كان يعيد تركيبه مع كل ضغطة مفتاح،
  فيفقد الحقل التركيز بعد كل حرف. نُقل إلى نطاق الوحدة.
- `Drawer.Content` مع `className` عرض خاص كان يصطدم بتخطيط HeroUI فيطفو الدرج
  بعيداً عن الحافة بمقدار ٨٤ بكسل. أُزيل العرض المخصّص.
- **رسم SVG بلا `width`/`height` صريحين لا حجم ذاتي له**، فيتقلّص إلى صفر مع
  `w-auto`. الحلّ: خاصيّة `fit` على `Garment` — `box` تملأ الصندوق و
  `preserveAspectRatio` الافتراضي يعطي سلوك «contain» بلا قصّ.
- `Select` يمرّر `isInvalid` إلى عنصر DOM فيطلق تحذير React؛ رسالة المحافظة
  تُعرض يدوياً مع `aria-describedby`.
- الانتقال بين المسارات لم يكن يغلق درج السلة.

### حالة التحقّق
- الرئيسية والمنتج وإتمام الطلب: **صفر إخفاقات تباين** (٢٠٨ و ٨٤ و ٧٧ عقدة نصية).
- **صفر مناطق نقر** تحت ٢٤ بكسل بعد توسيع مسار التنقّل وزرّ الدفع.
- كل الحقول لها تسمية مرتبطة، والأخطاء مرتبطة بـ `aria-describedby`،
  والتنبيه بـ `role="alert"`، والتركيز ينتقل إلى أول حقل ناقص.
- لا تمرير أفقي على ٣٧٥ بكسل في المسارات الثلاثة.
- دورة شراء كاملة: إضافة ← سلة ← عنوان ← تأكيد برقم طلب ← تفريغ السلة.
- `npm run build` ينجح.

### تحذير باقٍ
`A PressResponder was rendered without a pressable child` — تحذير تطوير من
React Aria لأن `Drawer` مُدار من الخارج بلا زرّ فتح بداخله. لا أثر له على
السلوك ولا يظهر في الإنتاج.

## لوحة التحكّم `#/admin` (٢٢ أغسطس ٢٠٢٦)

ثمانية تبويبات: **الأقسام** · **النصوص** · **الكتالوج** · **القماش** · **الألوان** ·
**الصور** · **المقاسات** · **الطلبيات**.

### حدّان يجب ذكرهما لأي مستخدم
- **بلا خادم**: التعديلات والطلبات في `localStorage` لهذا المتصفّح وحده.
  الزائر يرى النصوص الافتراضية. أُضيف تصدير/استيراد JSON لنقل الإعدادات.
- **بلا تسجيل دخول**: من يعرف الرابط يفتح اللوحة. لم أبنِ حقل كلمة مرور
  لأنه يوحي بحماية غير موجودة.

### قرارات
- التخزين يحفظ **الفروق فقط** (`vitas.content.v1`)، فتعديل النصوص الافتراضية
  في الشيفرة يصل لمن لم يعدّل ذلك المفتاح.
- تبويب النصوص يُبنى من `TEXTS` مباشرةً — لا قائمة حقول مكتوبة يدوياً تتفارق عنه.
- **عنوان البطل ثلاثة مفاتيح** (`hero.title.a/em/b`) لأن الجزء الأوسط يحمل
  الخط المميّز تحته. دمجها في نصّ واحد يفقد توقيع الصفحة.
- النصّ الفارغ يبقى فارغاً، ولكل مفتاح زرّ «استرجاع الأصلي» فلا يضيع عنوان بالخطأ.
- **إخفاء قسم يزيل رابطه** من الترويسة والتذييل معاً — لا رابط يقود إلى فراغ.
- الطلبات القديمة بلا `status` تُطبَّع عند القراءة إلى `new`.
- الإلغاء حالة لا حذف؛ والحذف النهائي يسأل قبل التنفيذ.

### فخّ HeroUI: `Switch.Control` داخل `Switch.Content`
قسم Anatomy في التوثيق يضع `Control` خارج `Content`، وهذا يترك شريط المفتاح
**خارج الـ label فلا يستجيب للنقر**. مثال Basic يضعه بالداخل وهو الصحيح.
كذلك `min-h-6` على الشريط لأنه ٢٠ بكسل افتراضاً.

### الكتالوج والمقاسات في اللوحة
- **الكتالوج**: الاسم، الوصف/القصّة، السعر، تركيب القماش، الكمية (للإصدارات)،
  ومفتاح الظهور. الأصل يبقى في `catalog.js` واللوحة تحفظ الفروق فوقه.
- **ممنوع من التحرير عمداً**: `type` و `colors` و `models` — تقود رسم SVG
  والمعرض ومطابقة الصور، وتعديلها من اللوحة يكسر العرض لا يغيّره.
- **إخفاء منتج** يزيله من الشبكة **وتصبح صفحته «لم نجد هذه القطعة»** —
  قطعة غير معروضة لا تُباع من رابط مباشر.
- **السعر يُلتقط عند الإضافة إلى السلة** لا عند الدفع: تغيير السعر لاحقاً
  لا يمسّ طلباً قائماً، وهو السلوك الصحيح تجارياً.
- **جدول المقاسات** يُستبدل كاملاً عند التحرير (`sizes` مصفوفة صفوف)، مع
  إضافة صفّ وحذفه واسترجاع الأصل.

### ما لم يُشمل بعد
`FABRIC` (بطاقات القماش الأربع) في `Home.jsx`، وألوان القطع وصورها.

### التحقّق
- التبويبات الثلاثة: **صفر إخفاقات تباين**، صفر حقول بلا تسمية، لا تمرير أفقي.
- إخفاء قسم يخفيه من الصفحة ويحذف رابطه من الترويسة والتذييل — تحقّقت.
- تحرير نصّ ينعكس على الرئيسية ويحتفظ بالخط المميّز — تحقّقت.
- تغيير حالة الطلب يُحفظ ويظهر في الشارة — تحقّقت.
- حقول التحرير تحتفظ بالتركيز أثناء الكتابة (كل التبويبات).
- تعديل اسم منتج وسعره ينعكس على البطاقة وصفحة المنتج — تحقّقت.
- إخفاء منتج يزيله من الشبكة ويحوّل صفحته إلى «لم نجد هذه القطعة» — تحقّقت.
- تحرير خليّة مقاس وإضافة صفّ وحذفه ينعكس على الجدول في الرئيسية — تحقّقت.
- «استرجاع الأصلي» يحذف مفتاح الفروق فيعود الافتراضي — تحقّقت للنصوص والكتالوج والمقاسات.
- تبويبا الكتالوج والمقاسات: صفر إخفاقات تباين، صفر حقول بلا تسمية، لا تمرير أفقي.

## المسودّة والنشر والمعاينة (٢٢ أغسطس ٢٠٢٦)

**تغيير في سلوك اللوحة**: التعديلات لم تعد تُطبَّق فوراً.

| المفتاح | المحتوى |
|---|---|
| `vitas.content.v1` | **المنشور** — ما يراه الزائر |
| `vitas.content.draft.v1` | **المسودّة** — ما تحرّره اللوحة |
| `vitas.preview` (session) | وضع المعاينة |

- `useContent()` تُرجع بيانات الواجهة محلولةً: `preview ? draft : published`،
  و `useContent().draft` تُرجع المسودّة دائماً — **اللوحة تستعملها حصراً**.
  خالف ذلك وستُظهر اللوحة بيانات المنشور كلّما أُطفئت المعاينة.
- **المسودّة تبدأ من المنشور لا من الفراغ**: البدء فارغاً يُظهر الافتراضيات في
  اللوحة بينما الموقع يعرض التعديلات، وأوّل حفظ يمحوها.
- `hasChanges` مقارنة نصّية بين المسودّة والمنشور، تقود تفعيل «حفظ ونشر».
- **شريط المعاينة** ثابت أحمر على الواجهة: بلا إعلان صريح يظنّ المستخدم أن
  تعديلاته منشورة. علم المعاينة في `sessionStorage` فلا يتسرّب إلى زائر حقيقي.

## القماش والألوان والمحرّر المنبثق

### بطاقات القماش
`fabric` مصفوفة `[رمز، عنوان، شرح]` تُستبدل كاملةً، مع إضافة بطاقة وحذفها.

### الألوان
- تعديل `hex` وحده **يعيد اشتقاق الظلّ والفاتح** منه (`deriveShade` / `deriveLight`).
  بدون ذلك يبقى التظليل من اللون القديم فيبدو الرسم غير متماسك.
- **الحذف غير متاح عمداً**: السلال والطلبات المحفوظة تشير إلى اللون بمفتاحه.
  و `colorway(key)` لا تُرجع `undefined` أبداً بل لوناً محايداً.
- كل مستهلكي `COLORWAYS` (سبعة ملفات) يمرّون الآن عبر `colorway()`.

### المحرّر المنبثق
`ItemEditor` نافذة واحدة للإضافة والتعديل، للمنتجات والإصدارات.
- **التحقّق**: رمز فريد بصيغة `XX-00`، اسم، سعر > صفر، **ولون واحد على الأقل**
  — `p.colors[0]` يُقرأ في بطاقة الشبكة وفي تهيئة الاختيار، وبطاقة بلا ألوان تسقط.
- الرمز المكرّر مرفوض: `findProduct` تستعمل `.find()` فيحجب المكرّر الأصل بصمت.
- المنتج المضاف يُخزَّن كاملاً في `ov.added`؛ والقطعة الأصلية فروقاً فقط.
- **`Modal` أناتومي**: `Modal > Backdrop > Container > Dialog > (CloseTrigger, Header>Heading, Body, Footer)`.
- `isInvalid` لا يُمرَّر إلى `TextArea` — ينتهي على عنصر DOM ويطلق تحذير React.

### سطح النوافذ
`Modal.Dialog` و `Drawer.Dialog` يأتيان أبيض ناصعاً من HeroUI، وهو يقطع مع ورق
الصفحة. أُعيدا إلى `--color-paper` بحدّ حادّ في `theme.css`.

### درس في أداة التدقيق
قارئ الألوان في فحص التباين كان يتجاهل `oklch()` فيعدّها شفّافة ويقيس النصّ على
الحجاب الأسود — خمسة عشر «إخفاقاً» وهمياً في المودال. القارئ الصحيح يمرّ اللون
عبر `canvas` فيفهم كل صيغ CSS:
```js
const x = document.createElement('canvas').getContext('2d');
x.fillStyle = str; x.fillRect(0, 0, 1, 1);
const [r, g, b, a] = x.getImageData(0, 0, 1, 1).data;
```

### التحقّق
- دورة كاملة: تعديل ← المسودّة تتغيّر والمتجر لا ← معاينة تُظهر المسودّة ←
  نشر ← المتجر يعرضها. تحقّقت لنصّ وللكتالوج.
- إضافة لون `mint` واستعماله في منتج جديد `VS-07`: يظهر في الشبكة وفي صفحته
  ويُرسم بالألوان المشتقّة (`#B5651D` → ظلّ `#824915` وفاتح `#C78A53`).
- التحقّق من النموذج يمنع الحفظ ويعرض أربع رسائل، ويرفض الرمز المكرّر.
- التبويبات الجديدة والمودال: **صفر إخفاقات تباين**، صفر حقول بلا تسمية.
- كل الحقول تحتفظ بالتركيز أثناء الكتابة. `npm run build` ينجح بلا أخطاء طرفية.

## صور الموديل من اللوحة (٢٣ أغسطس ٢٠٢٦)

### الربط
منتقٍ لكل قَصّة داخل نافذة تعديل المنتج. **الترتيب يتبع ترتيب المكتبة لا ترتيب
النقر**: `ToggleButtonGroup` يُرجع `Set` بلا ضمان ترتيب، وأوّل صورة هي اللقطة
الافتراضية في المعرض — فالترتيب ليس تفصيلاً.

`galleryShots(p, fit, photoOf)` تأخذ المحلِّل وسيطاً لأنها دالّة مصدَّرة لا تستطيع
استعمال hook. ومرشّح `.filter(x => x.src)` يُسقط أي مفتاح لم يعد في المكتبة بهدوء.

### الرفع
- **التصغير إلزامي لا تحسين**: حصّة `localStorage` نحو ٥ ميغابايت للموقع كلّه،
  وتُحسب مرّتين لأن المسودّة والمنشور نسختان. `downscaleImage` تصغّر إلى
  ١١٠٠ بكسل وتخفض الجودة تدريجياً حتى حدّ ٤٢٠ ك.ب.
- **القياس بعد الترميز لا قبله**: المهمّ حجم الـ data URL الناتج.
  (٢٠٠٠×٣٠٠٠ بحجم ١١٩ ك.ب → ٧٣٣×١١٠٠ بحجم ١١ ك.ب.)
- **امتلاء التخزين لم يعد يُبتلع بصمت**: `QuotaExceededError` يُرفع إلى
  `storageError` ويظهر بشريط أحمر في اللوحة. الابتلاع الصامت كان سيبدو
  للمستخدم كأن الصورة «اختفت».

### شرطان في نموذج الرفع
- **الوصف البديل إلزامي** (عشرة أحرف فأكثر): المعرض يعرضه وتدقيق الوصولية يفحصه.
- **اللون يُختار من قائمة الألوان لا نصّاً حرّاً**: منطق التعليق يقارن
  `cur.color === c.name` بالاسم المعروض، وأي اختلاف نصّي يجعل التعليق يقول دائماً
  «الصورة باللون س · لون القطعة ص» حتى لو تطابقا.

### الحذف والاستعمال
كل صفّ يعرض المنتجات التي تستعمل الصورة، والحذف يسأل ويذكرها بالاسم.
الحذف **للمرفوعة فقط** — الاثنتا عشرة المدمجة ثابتة.

### الترخيص
سطر التذييل صار «الصور **المدمجة** من Freepik» بعد أن اختلط المصدر:
ما ترفعه أنت ملكك، والالتزام يخصّ المدمجة.

### التصدير
`exportJSON` يشمل الصور المرفوعة، فالملف يكبر بحجمها — وهو مقصود ليبقى
النقل إلى جهاز آخر كاملاً.

### التحقّق
- ٢٦ مصغّرة في المنتقي (١٣ صورة × قَصّتين)، والاختيار يُحفظ مرتّباً بترتيب المكتبة.
- رفع صورة ٢٠٠٠×٣٠٠٠ → تصغير وتحقّق ورفض بلا وصف بديل ورفض بلا لون.
- منتج جديد `VS-08` بصورة مرفوعة: يعرضها في معرضه بوصفها البديل وتعليقها الصحيح،
  والقَصّة بلا صور تعرض «القطعة مسطّحة» وحدها.
- تبويب الصور: **صفر إخفاقات تباين**، صفر صور بلا `alt`، صفر حقول بلا تسمية.

## المعاينة الحيّة داخل اللوحة (٢٣ أغسطس ٢٠٢٦)

لوح يعرض الرئيسية نفسها مبنيّةً من **المسودّة**، بجانب المحرّر — بلا دخول
وضع المعاينة ولا مغادرة اللوحة. وضع المعاينة الكامل بقي لمن أراد الحجم الحقيقي.

### كيف يقرأ المسودّة
`useContent()` تحلّ `preview ? draft : published`، وهذا لا يصلح للوح داخل اللوحة:
يجب أن يعرض المسودّة دائماً. الحلّ `DraftScope` — مزوّد يعيد تقديم
`{...api, ...api.draftView}` لشجرته الفرعية فيرى `Home` بداخله بيانات المسودّة
مهما كان الوضع العام.

### قرارات
- **`zoom` لا `transform: scale`**: الأخير لا يؤثّر في التخطيط، فيحجز اللوح
  عرض ١٢٨٠ بكسل ويولّد تمريراً أفقياً. `zoom` يؤثّر فيه فيأخذ حجمه الحقيقي.
- **`inert` لا `aria-hidden` وحده**: الأخير يُخفي من قارئ الشاشة لكنه يترك
  **٦٧ عنصراً** في ترتيب التنقّل بلوحة المفاتيح. و`inert=""` سلسلة فارغة
  تُسقطها React كقيمة زائفة — الصحيح `inert={true}`.
- التبويب المفتوح يمرّر اللوح إلى قسمه (`TAB_FOCUS`)؛ `''` تعني أظهر بلا تمرير،
  و`null` تعني لا معاينة (الطلبيات بيانات تشغيل لا محتوى صفحة).

### فخّ الشبكة على الشاشات الضيّقة
عناصر الشبكة ومساراتها تُقاس افتراضياً بـ `auto`، فقائمة التبويبات الثمانية
دفعت العمود إلى ٥٩٧ بكسل على شاشة ٣٧٥ وأحدثت تمريراً أفقياً.
اللازم **الاثنان معاً**: `min-w-0` على عنصر الشبكة، و`grid-cols-[minmax(0,1fr)]`
على المسار نفسه — أحدهما وحده لا يكفي.

### التحقّق
- إخفاء قسم / تعديل نصّ / تغيير لون: كلّها تنعكس في اللوح **فوراً**،
  والمنشور لا يتغيّر (`vitas.content.v1` يبقى كما هو).
- تغيير اللون يظهر بظلّه وفاتحه المشتقّين داخل رسوم اللوح.
- الانتقال بين التبويبات يمرّر اللوح إلى القسم الصحيح.
- `inert`: ٦٧ عنصراً قابلاً للتركيز → **صفر**.
- صفر إخفاقات تباين، ولا تمرير أفقي على ٣٧٥ و٩٠٠ و١٤٤٠ بكسل.

## النشر على GitHub Pages (٨ سبتمبر ٢٠٢٦)

المستودع: `Alshaikhly97/zahra` (عام) — والموقع على
**https://alshaikhly97.github.io/zahra/**

النشر آلي: كل دفع إلى `main` يشغّل `.github/workflows/deploy.yml`
فيبني بـ `npm ci && npm run build` ويرفع `dist` كأثر Pages.
مصدر Pages هو **GitHub Actions** لا فرعاً (`build_type: workflow`)،
فلا وجود لفرع `gh-pages` ويبقى `dist/` في `.gitignore`.

### الارتباط الذي يكسر الموقع بصمت
`base: '/zahra/'` في `vite.config.js` **يجب أن يطابق اسم المستودع**.
تغيير اسم المستودع بلا تغييرها يجعل كل الأصول تُطلب من جذر النطاق
`alshaikhly97.github.io/assets/…` فتسقط ٤٠٤ — والصفحة تظهر بيضاء بلا خطأ مفهوم.

ولهذا **مسارات الصور المدمجة تمرّ عبر `import.meta.env.BASE_URL`**
(الثابت `IMG` في `catalog.js`) لا `/img/…` المطلق الذي كان يعمل محلياً
على الجذر ويسقط تحت المسار الفرعي. الصور المرفوعة من اللوحة `data:` فلا تتأثر.

الفحص الذي يميّز — بعد `npm run build` وقبل الدفع:
```bash
grep -o '`/zahra/img/`' dist/assets/*.js   # يجب أن يوجد، بشرطة واحدة
grep -o '"/img/[^"]*"' dist/assets/*.js    # يجب ألّا يوجد شيء
```
«البناء نجح» ليس فحصاً — البناء ينجح والمسارات خاطئة.

### الموجّه بالهاش يوفّر خطوة
`#/product/VS-01` لا يصل إلى خادم Pages أصلاً، فلا حاجة إلى نسخة `404.html`
ولا إعادة كتابة مسارات. لا تُضِفها.

### قالب GitHub الرسمي متأخّر
`actions/starter-workflows` يثبّت `checkout@v4` و`setup-node@v4` و
`upload-pages-artifact@v3` — وكلّها تُطلق تحذير إهمال Node 20.
الملفّ يستعمل الإصدارات الحالية (`v7`/`v7`/`v6`/`v5`). تحقّق بـ
`gh api repos/actions/checkout/releases/latest` لا من الذاكرة.

### ما بقي مفتوحاً بعد النشر العام
- **ترخيص صور Freepik**: الموقع صار عاماً فعلاً. سطر التذييل يذكر المصدر،
  لكن الصيغة المطلوبة لكل صورة **لم تُتحقَّق بعد**.
- **`#/admin` مفتوحة للعموم**: من يعرف الرابط يفتح اللوحة. لا تسريب —
  التخزين محلّي لمتصفّح الزائر وحده، وتعديلاته لا تصل إلى أحد —
  لكنها ظاهرة في الترويسة والتذييل لكل زائر.
- **الطلبات لا تصل إليك**: تُحفظ في متصفّح الزبون. المتجر معرض لا قناة بيع
  حتى يُضاف خادم.
